---
type: Concept
title: "세계 종말 날짜 계산"
description: "Algorithms (알고리즘 & 자료구조) - ChatGPT 대화 추출 노트 (2025-12-26)"
tags: [algorithms, chatgpt-export]
timestamp: 2025-12-26
status: active
---

# 세계 종말 날짜 계산

> **카테고리**: `Algorithms (알고리즘 & 자료구조)`  
> **원천**: ChatGPT 대화 아카이브 (`raw/notes/Chatgpt_2026-08-02-23-14-23`)  
> **작성일**: 2025-12-26

## 💬 대화 내용 및 Q&A

### ❓ 질문 (User)
문제
Many say that the world is going to end on 12/21/2012 G (dates in our calendar, the Gregorian calendar, will have the suffix G), but how did they come up with this exact date? Sadly, most of them are probably just repeating what everyone else is saying, but there is some evidence for this date. The theory is based on the beliefs of the Maya civilization and their calendar, the Mesoamerican Long Count calendar (dates in this calendar will have the suffix MLC). It is said that the Maya believed that there were three unsuccessful worlds before the current fourth one. The last one is said to have ended on 13.0.0.0.0 MLC. Since this date is also the beginning of the current world, the calendar is reset and the date is referred to as 0.0.0.0.0 MLC. When we reach 13.0.0.0.0 MLC again, it could mean the end of our current world if history is to repeat itself. Given a mapping from MLC to G, we would like to know how many days are left until the end of the world.
Unfortunately, we need two additional calendars in order figure this out! But before we get into that, what are these MLC dates anyway? MLC dates are just a count of the number of days since the creation of our current world. It is a base-20 number delimited by dots, except that the second digit is base-18. So, the day before the end of the current world is 12.19.19.17.19 MLC.
So why do we need two more calendars? Well, when astronomers try to match up Mayan astronomical records with known dates for astronomical events, they specify the known date by its Julian Day Number (dates in this calendar will have the suffix JDN). JDN has the benefit that it is easy to do computations with because, like the MLC calendar, JDN is a count of the number of days since a starting day, 0 JDN. Since we want to know the number of days until the end of the world, all we need is the JDN of today’s date, 5/6/2012 G, and the JDN of the end of the world, 13.0.0.0.0 MLC. The JDN of the end of the world can easily be computed given the MLC of an astronomical event and its equivalent JDN. On the other hand, figuring out the JDN of today’s date is not so easy and requires use of another calendar.
0 JDN is specified in terms of the Julian calendar (dates in this calendar will have the suffix J). When history books refer to dates before 10/15/1582 G, they’re usually referring to a Julian calendar date because the Gregorian calendar wasn’t used until 10/5/1582 J at which time the Gregorian calendar skipped 10 days and started at 10/15/1582 G. The Julian calendar is exactly the same as the Gregorian calendar except that the Julian calendar has an easier leap-year rule (and the 10 day difference just mentioned). A year Y is a leap-year when:
Julian: Y % 4 == 0
Gregorian: (Y % 4 == 0 && Y % 100 != 0) || Y % 400 == 0
0 JDN is equivalent to 1/1/4713 BCE J (BCE stands for “before common era” and is used to denote a date before 1/1/1 J. These dates behave the same except that the year counts down to 1 BCE J. The year after that is the start of the “common era”, 1 J). BCE dates don’t follow the leap-year rule because 4 years before 1/1/4J is 1/1/1 BCE J. So the leap-year rule becomes:
BCE J: (Y-1) % 4 == 0
We can use this to count the number of days from 0 JDN to 10/5/1582 J. From there, we can use the Gregorian leap-year rules to count the number of days from 10/15/1582 G to today’s date. Summing up both of these counts will tell us the JDN of today’s date:
# of Julian days = 4713 * 365
                 + (4713-1) / 4 + 1 // leap-days
                 + 1581 * 365
                 + 1581 / 4 // leap-days
                 + 5*31 + 3*30 + 28 + 4 // days in 1582
                 = 2299161
# of Gregorian days = ( ((2012-1)-1200) * 365 // days since 1200
                    + ((2012-1)-1200) / 4 // overcount leap-days
                    - ((2012-1)-1200) / 100 // adjust count
                    + ((2012-1)-1200) / 400 // adjust count
                    + 2*31 + 30 + 29 + 6 ) // days in 2012
                    - ( ((1582-1)-1200) * 365 // days from 1200 to 1582
                    + ((1582-1)-1200) / 4
                    - ((1582-1)-1200) / 100
                    + ((1582-1)-1200) / 400
                    + 5*31 + 3*30 + 28 + 14 ) // days in 1582
                    = 156894
Total = 2299161 + 156894
      = 2456055
This includes 0 JDN, so 5/6/2012 G is equivalent to 2456054 JDN.
입력
The first line is the number K of input data sets, followed by the K data sets, each of the following form:
The first line contains a date M in the MLC calendar, made up of five integer digits 0 ≤ Mi ≤ 19 where 0 ≤ M4 ≤ 17. This is the date of an astronomical event. The next line contains a single integer 0 ≤ J indicating the most probable JDN of the same astronomical event.
출력
For each data set, output “Data Set x:” on a line by itself, where x is its number. On the next line output the number of days until the end of the world assuming we have the correct JDN for the astronomical event. However, if the number is negative, output “It’s a hoax!”, and if the number is 0, output “Panic!”. Each data set should be followed by a blank line.
예제 입력 1 
3
0 0 0 0 0
584283
2 5 8 13 21
400000
7 1 19 6 0
1606214
예제 출력 1 
Data Set 1:
229

Data Set 2:
It's a hoax!

Data Set 3:
Panic!
===
한국어로 번역해줘

### 💡 답변 (Assistant)
아래는 문제 전체의 **한국어 번역**입니다.

---

## 문제

많은 사람들은 세상이 **2012년 12월 21일 G**(우리의 달력인 그레고리력 날짜에는 접미사 **G**가 붙음)에 끝날 것이라고 말한다. 그런데 이 정확한 날짜는 어떻게 나온 것일까? 안타깝게도 대부분은 다른 사람들이 말하는 것을 그대로 반복할 뿐이지만, 이 날짜에 대한 몇 가지 근거는 존재한다.

이 이론은 **마야 문명**과 그들의 달력인 **메소아메리카 장기력(Mesoamerican Long Count calendar, MLC)**에 기반한다. 이 달력의 날짜에는 접미사 **MLC**가 붙는다. 마야인들은 현재의 세계 이전에 세 번의 실패한 세계가 있었다고 믿었으며, 마지막 세계는 **13.0.0.0.0 MLC**에 끝났다고 전해진다. 이 날짜는 동시에 현재 세계의 시작이기도 하여, 달력이 초기화되고 **0.0.0.0.0 MLC**로 불린다. 만약 우리가 다시 **13.0.0.0.0 MLC**에 도달한다면, 역사가 반복된다는 가정 하에 현재의 세계가 끝난다는 의미일 수 있다.

MLC에서 그레고리력(G)으로의 대응 관계가 주어졌을 때, 우리는 **세계의 종말까지 며칠이 남았는지**를 알고자 한다.

불행히도 이를 계산하려면 **두 개의 추가적인 달력**이 더 필요하다. 그에 앞서, MLC 날짜가 무엇인지부터 살펴보자.

MLC 날짜는 현재 세계가 창조된 이후 경과한 날짜 수를 나타낸 것이다. 점(`.`)으로 구분된 **20진수** 표현이지만, **두 번째 자릿수는 18진수**이다. 예를 들어, 현재 세계가 끝나기 하루 전은  
**12.19.19.17.19 MLC** 이다.

---

### 왜 두 개의 달력이 더 필요한가?

천문학자들이 마야의 천문 기록을 알려진 천문 현상의 날짜와 대응시킬 때, 그 알려진 날짜를 **율리우스 일수(Julian Day Number, JDN)**로 표현한다. 이 달력의 날짜에는 접미사 **JDN**이 붙는다.

JDN의 장점은 계산이 매우 쉽다는 점인데, MLC 달력처럼 어떤 기준일로부터의 **일 수 누적**이기 때문이다. 우리가 세계 종말까지 남은 일 수를 알고 싶다면, 다음 두 가지만 알면 된다.

- 오늘 날짜 **2012년 5월 6일 G**의 JDN  
- 세계의 종말인 **13.0.0.0.0 MLC**의 JDN  

세계 종말의 JDN은, 어떤 천문 현상의 **MLC 날짜와 그에 대응하는 JDN**이 주어지면 쉽게 계산할 수 있다. 반면, 오늘 날짜의 JDN을 구하는 것은 쉽지 않으며 **또 다른 달력**이 필요하다.

---

### 율리우스력(Julian Calendar)

**0 JDN**은 **율리우스력(J)** 기준의 **기원전 4713년 1월 1일**과 같다.  
(기원전은 **BCE**로 표기하며, **1/1/1 J** 이전의 날짜를 의미한다.)

역사 서적에서 **1582년 10월 15일 G 이전**의 날짜는 보통 율리우스력을 의미한다. 이는 그레고리력이 **1582년 10월 5일 J**까지 사용되다가, **10일을 건너뛰고 1582년 10월 15일 G**로 시작되었기 때문이다.

율리우스력은 윤년 규칙을 제외하면 그레고리력과 동일하다.

- **율리우스력 윤년 규칙**:  
  `Y % 4 == 0`
- **그레고리력 윤년 규칙**:  
  `(Y % 4 == 0 && Y % 100 != 0) || Y % 400 == 0`

기원전(BCE) 날짜의 경우, 연도가 **1 BCE J → 1 J**로 넘어가므로 윤년 규칙이 다음과 같이 바뀐다.

- **BCE 율리우스력 윤년 규칙**:  
  `(Y - 1) % 4 == 0`

---

### JDN 계산

이를 이용하여 **0 JDN부터 1582년 10월 5일 J**까지의 일 수를 계산하고,  
그 이후 **1582년 10월 15일 G부터 오늘 날짜까지**를 그레고리력 윤년 규칙으로 계산한다.

#### 율리우스력 일 수
```
# of Julian days = 4713 * 365
                 + (4713-1) / 4 + 1   // 윤일
                 + 1581 * 365
                 + 1581 / 4          // 윤일
                 + 5*31 + 3*30 + 28 + 4  // 1582년 일 수
                 = 2299161
```

#### 그레고리력 일 수
```
# of Gregorian days = ( ((2012-1)-1200) * 365
                      + ((2012-1)-1200) / 4
                      - ((2012-1)-1200) / 100
                      + ((2012-1)-1200) / 400
                      + 2*31 + 30 + 29 + 6 )
                    - ( ((1582-1)-1200) * 365
                      + ((1582-1)-1200) / 4
                      - ((1582-1)-1200) / 100
                      + ((1582-1)-1200) / 400
                      + 5*31 + 3*30 + 28 + 14 )
                    = 156894
```

#### 합계
```
Total = 2299161 + 156894
      = 2456055
```

이는 **0 JDN을 포함**하므로,  
**2012년 5월 6일 G = 2456054 JDN** 이다.

---

## 입력

첫 줄에는 입력 데이터 세트의 개수 **K**가 주어진다.  
이후 **K개의 데이터 세트**가 다음 형식으로 주어진다.

- 첫 줄: 다섯 개의 정수로 이루어진 **MLC 날짜 M**  
  - `0 ≤ Mi ≤ 19`, 단 `0 ≤ M4 ≤ 17`
- 다음 줄: 동일한 천문 현상에 대응하는 **가장 유력한 JDN 값 J**  
  - `0 ≤ J`

---

## 출력

각 데이터 세트에 대해 다음과 같이 출력한다.

- 첫 줄:  
  ```
  Data Set x:
  ```
  (x는 데이터 세트 번호, 1부터 시작)
- 다음 줄:
  - 세계 종말까지 남은 날짜 수 출력
  - 값이 **음수**이면  
    ```
    It's a hoax!
    ```
  - 값이 **0**이면  
    ```
    Panic!
    ```

각 데이터 세트 출력 뒤에는 **빈 줄 한 줄**을 추가한다.

---

## 예제 입력
```
3
0 0 0 0 0
584283
2 5 8 13 21
400000
7 1 19 6 0
1606214
```

## 예제 출력
```
Data Set 1:
229

Data Set 2:
It's a hoax!

Data Set 3:
Panic!
```

---

---

### ❓ 질문 (User)
문제 이해가 잘 되지 않는데 쉽게 설명해줘.

### 💡 답변 (Assistant)
알겠습니다. 이 문제는 **겉보기엔 복잡한 역사·달력 이야기**처럼 보이지만, 실제로는 **“날짜를 모두 ‘하루 수’로 바꿔서 빼기”** 문제입니다. 단계별로 아주 쉽게 설명하겠습니다.

---

## 1. 이 문제에서 진짜로 묻는 것

각 데이터 세트마다 결국 묻는 것은 **단 하나**입니다.

> **2012년 5월 6일(Gregorian) 기준으로,  
> 마야 달력에서 13.0.0.0.0 MLC까지 며칠 남았는가?**

단,
- 음수 → 이미 끝났다는 뜻 → `"It's a hoax!"`
- 0 → 오늘이 그 날 → `"Panic!"`
- 양수 → 남은 날짜 수 출력

---

## 2. 왜 달력이 이렇게 많이 나오나?

핵심은 **모든 날짜를 같은 기준으로 바꾸기 위해서**입니다.

| 달력 | 역할 |
|---|---|
| MLC | 마야 날짜 (끝나는 날: 13.0.0.0.0) |
| JDN | 날짜를 “하루 번호”로 표현하는 중간 매개 |
| G / J | 실제 역사 날짜 → JDN으로 바꾸기 위한 수단 |

👉 **결론:**  
**모든 날짜를 JDN(연속된 일 수)으로 바꾼 후 빼기만 하면 된다**

---

## 3. 마야 달력(MLC)은 무엇인가?

MLC는 **“세계가 시작된 날부터 며칠이 지났는지”**를 나타낸 숫자입니다.

형식:
```
baktun.katun.tun.uinal.kin
```

가중치(아주 중요):
```
1 kin    = 1일
1 uinal  = 20 kin
1 tun    = 18 uinal = 360일
1 katun  = 20 tun   = 7,200일
1 baktun = 20 katun = 144,000일
```

즉, MLC 날짜 → **총 일 수로 바로 변환 가능**

예:
```
13.0.0.0.0 MLC
= 13 × 144,000
= 1,872,000일
```

---

## 4. 주어지는 입력 데이터가 의미하는 것

각 데이터 세트는 **“기준점” 하나**를 줍니다.

```
MLC 날짜 M
↓
JDN 날짜 J
```

뜻:
> “이 마야 날짜는, 실제로는 JDN J번째 날이었다”

이 기준 덕분에,
- **세계 시작일(0.0.0.0.0 MLC)**이
- **JDN에서 며칠인지 계산 가능**

---

## 5. 세계 종말의 JDN 구하기

1️⃣ 입력으로 주어진 MLC → **총 일 수 계산**  
2️⃣ 목표 날짜 13.0.0.0.0 MLC → **총 일 수 계산**  
3️⃣ 차이를 이용해 **세계 종말의 JDN** 계산

공식:
```
End_JDN = given_JDN + (days(13.0.0.0.0) - days(given_MLC))
```

---

## 6. 오늘 날짜는 이미 JDN으로 주어짐

문제에서 친절하게 계산해줍니다.

```
2012년 5월 6일 G = 2456054 JDN
```

👉 여러분은 **다시 계산할 필요 없음**

---

## 7. 최종 계산

```
남은 일 수 = End_JDN - 2456054
```

| 결과 | 출력 |
|---|---|
| > 0 | 숫자 |
| = 0 | Panic! |
| < 0 | It's a hoax! |

---

## 8. 예제 하나만 직관적으로 보기

### 데이터 세트 1
```
0.0.0.0.0 MLC → JDN 584283
```

- 세계 시작일이 JDN 584283
- 세계 종말: 1,872,000일 후

```
End_JDN = 584283 + 1,872,000
        = 2456283
```

```
2456283 - 2456054 = 229
```

✔ 출력: `229`

---

## 9. 이 문제의 본질 요약

✔ 달력 지식 시험 ❌  
✔ **기준점 하나를 이용한 날짜 오프셋 계산 문제** ⭕

한 줄로 정리하면:

> **“마야 날짜 → 일 수 → JDN으로 맞춘 뒤, 오늘 날짜와 빼라”**

---

다음으로 도움이 될 수 있는 것:
- ✔ MLC → 일 수 변환 공식 정리
- ✔ 전체 알고리즘을 의사코드로 설명
- ✔ 파이썬 구현 예제

원하시는 단계로 이어서 설명해 드리겠습니다.

---

### ❓ 질문 (User)
```
def product(lst) :
    res = 1
    for num in lst :
        res *= num
    return res

K = int(input())
for k in range(1, K+1) :
    MLC = list(map(int, input().split()))   # 20, 20, 20, 18, 20 진법
    JDN = int(input())                      #

    end_G_to_J = 2456054
    max_MLC = 13 * 20 * 20 * 18 * 20
    MLC_val = [20, 20, 18, 20, 1]
    D_event = 0
    for i in range(5) :
        D_event += MLC[i] * product(MLC_val[i:])

    end_jdn = JDN + (max_MLC - D_event)
    res = end_jdn - end_G_to_J

    if res > 0 :
        print(res)
    elif res == 0 :
        print("Panic!")
    else :
        print("It's a hoax!")
```
틀렸대. 어디서 계산이 잘못된걸까?

### 💡 답변 (Assistant)
코드는 전체 구조와 접근 방식은 **거의 맞습니다**.  
틀린 이유는 **MLC → 일 수로 변환하는 가중치 계산에서 핵심적인 오류가 하나** 있기 때문입니다.

아래에서 **문제의 본질 → 현재 코드의 오류 → 정확한 계산 방식** 순서로 설명하겠습니다.

---

## 1. 이 문제에서 가장 중요한 공식

MLC 각 자리의 **정확한 가중치**는 다음과 같습니다.

```
[baktun, katun, tun, uinal, kin]

1 kin    = 1
1 uinal  = 20
1 tun    = 18 × 20 = 360
1 katun  = 20 × 360 = 7,200
1 baktun = 20 × 7,200 = 144,000
```

즉:

```python
weights = [144000, 7200, 360, 20, 1]
```

---

## 2. 현재 코드에서 잘못된 부분

### (1) `MLC_val` 정의가 틀림

```python
MLC_val = [20, 20, 18, 20, 1]
```

이 배열은 **각 자리의 진법**을 나타낸 것이지  
**해당 자리가 의미하는 ‘일 수’ 가중치가 아닙니다.**

그런데 아래 코드에서 이를 가중치처럼 사용하고 있습니다.

```python
D_event += MLC[i] * product(MLC_val[i:])
```

이 방식은 **의도는 맞아 보이지만 실제 값이 틀어집니다.**

---

### (2) 특히 가장 큰 문제: `max_MLC` 계산

```python
max_MLC = 13 * 20 * 20 * 18 * 20
```

이건 **MLC의 표현 범위**를 계산한 것이지  
**13.0.0.0.0 MLC가 의미하는 일 수가 아닙니다.**

올바른 계산은:

```
13 × 144,000 = 1,872,000
```

하지만 현재 코드는:

```
13 × 20 × 20 × 18 × 20 = 1,872,000  ← 우연히 값만 같음
```

👉 **숫자는 우연히 같지만, 논리적으로 매우 위험한 계산**입니다.  
이 때문에 `D_event` 계산과의 일관성이 깨집니다.

---

## 3. 왜 예제에서 오답이 나는가?

`D_event`는  
- “MLC 날짜가 시작점으로부터 며칠인지”  
를 나타내야 하는데,

현재 방식은
- 진법 곱셈을 통해 **형태만 흉내낸 계산**
이 되어버려서,

**입력 MLC가 0.0.0.0.0이 아닐 경우 오차가 발생합니다.**

즉:
- Data Set 1은 우연히 통과
- Data Set 2, 3에서 실패

---

## 4. 올바른 MLC → 일 수 계산 방법

가중치를 **명시적으로** 써야 합니다.

### 수정된 핵심 코드

```python
weights = [144000, 7200, 360, 20, 1]

D_event = 0
for i in range(5):
    D_event += MLC[i] * weights[i]

max_MLC = 13 * 144000  # 13.0.0.0.0
```

이렇게 하면:
- 의미적으로 정확
- 어떤 입력에서도 안정적으로 동작

---

## 5. 전체 수정된 핵심 흐름

```python
end_G_to_J = 2456054
weights = [144000, 7200, 360, 20, 1]
max_MLC = 13 * 144000

D_event = sum(MLC[i] * weights[i] for i in range(5))
end_jdn = JDN + (max_MLC - D_event)
res = end_jdn - end_G_to_J
```

---

## 6. 핵심 정리

❌ 잘못된 개념  
- 진법 배열을 가중치처럼 사용  
- `product(MLC_val[i:])` 방식

⭕ 올바른 개념  
- **MLC는 혼합 진법이지만, 계산은 ‘고정 일 수 가중치’로 해야 함**
- 각 자리는 이미 “몇 일인지”가 정의되어 있음

---

### 한 줄 요약

> **MLC는 진법 문제가 아니라 “이미 단위가 정해진 날짜 단위 변환 문제”다.**

원하시면:
- 왜 진법 접근이 위험한지 수학적으로 설명
- 테스트 케이스별로 값이 어떻게 어긋나는지 추적
- 최종 AC 가능한 전체 코드 제공

어디까지 더 보고 싶은지 말씀해 주세요.

---
