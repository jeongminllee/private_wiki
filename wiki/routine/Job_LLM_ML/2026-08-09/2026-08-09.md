## 새로 확인된 고적합 공고 — 2026년 8월 9일

오늘은 **신규 고적합 공고 1건**만 알림 대상으로 선별했습니다. 모델 학습뿐 아니라 **vLLM/SGLang 서빙, GPU 클러스터, 분산 학습, SFT·DPO·GRPO, Hugging Face, RAG·Agent**를 한 포지션에서 모두 다룬다는 점에서 지금까지 추적해 온 직무군과 상당히 정확하게 겹칩니다. ([LinkedIn][1])

### Babechat.ai — LLM/AI Engineer

* **근무지:** 경기도, 주 2일 오피스 출근 + 나머지 원격 근무
* **고용 형태:** 정규직·풀타임
* **요구 경력:** 공식 Babechat 채용 페이지 기준 **경력 3년 이상**. 다만 LinkedIn에는 직급이 `신입`으로 표시되어 있어 두 채널의 분류가 상충합니다. 지원 판단에는 회사 공식 페이지의 3년 이상 조건을 우선해서 보는 것이 안전합니다.
* **게시 시점:** LinkedIn 기준 **2026년 8월 6일 전후**, 8월 9일 현재 모집 중
* **마감:** 별도 마감일 미표기
* **지원:** Babechat **공식 채용 페이지**에서 지원 가능. ([Babechat][2])

이 포지션의 업무 범위가 꽤 강합니다. LLM inference infrastructure 설계와 GPU 클러스터 모델 서빙·성능 튜닝부터 시작해서, distributed training, alignment용 데이터 파이프라인, SFT·DPO·GRPO, RAG, context engineering, LLM Agent까지 **LLM의 전체 lifecycle을 한 엔지니어가 다루는 구조**입니다. 특히 자격요건은 모든 영역의 경험을 요구하는 것이 아니라 `vLLM/SGLang inference`, `GRPO/SFT/DPO training`, `RAG/Agent` 중 **최소 한 영역의 실무 경험**을 요구합니다. ([LinkedIn][1])

### 왜 잘 맞는가

현재 추적 조건과의 기술 겹침은 상당히 높습니다. 공고가 **PyTorch 분산 학습과 Hugging Face 생태계를 명시적으로 우대**하고 있으며, 모델 inference 쪽에서는 vLLM·SGLang과 GPU 성능 튜닝, 학습 쪽에서는 alignment와 distributed training을 요구합니다. 즉 Python/PyTorch/Hugging Face 중심의 모델 튜닝 역량을 ML Infra 쪽으로 확장하려는 지원자에게 특히 좋은 형태입니다. ([LinkedIn][1])

또한 단순히 API를 연결하는 GenAI 포지션이 아니라 **모델을 직접 학습하고 → GPU에서 서빙하고 → 성능을 최적화하고 → 실제 LLM 서비스까지 운영하는 역할**이라는 점이 중요합니다. Kubernetes·Docker·클라우드, quantization, Vector DB, Function Calling과 Tool Use도 우대사항으로 잡혀 있어 ML 시스템 쪽 포트폴리오를 그대로 활용하기 좋습니다. ([LinkedIn][1])

### 가장 큰 약점

문제는 **공식 페이지의 경력 3년 이상 조건**입니다. LinkedIn은 이 포지션을 신입으로 분류하지만 Babechat 자체 채용 페이지는 명확히 `경력 3년 이상`으로 표시하고 있기 때문에, 실제 서류 심사에서는 경력직 기준을 적용할 가능성이 높습니다. 따라서 이 공고는 현실적으로는 **즉시 합격권이라기보다 기술 적합도가 높은 도전 지원**으로 보는 것이 맞습니다. ([Babechat][3])

또 하나는 공고가 요구하는 “실무 경험”입니다. 개인 프로젝트라도 단순히 `vLLM으로 모델을 실행했다` 수준보다는 **성능 문제를 측정하고 개선했다는 증거**가 있어야 경쟁력이 생깁니다. 수백만 사용자 규모의 실제 서비스 경험까지 따라갈 필요는 없지만, 적어도 throughput·latency·VRAM·동시 요청 처리량 같은 수치가 있어야 합니다. 해당 역할 자체가 대규모 inference와 성능 최적화를 핵심 업무로 명시하고 있습니다. ([LinkedIn][1])

### 지원 전에 가장 효과적인 준비

이 공고 때문에 별도의 대형 프로젝트를 새로 만드는 것보다 **LLM Serving + Training Benchmark 하나를 완성하는 편이 훨씬 효율적**입니다. 동일한 7B~14B급 모델을 Transformers와 vLLM에서 실행한 뒤 TTFT, TPOT, tokens/s, peak VRAM, 동시 요청 증가에 따른 p95 latency를 측정하고, AWQ 또는 FP8/INT8 같은 quantization을 적용해 전후 결과를 남기는 것이 좋습니다. 그다음 LoRA SFT 또는 DPO 한 번을 추가해 `학습 → 모델 생성 → vLLM 배포 → benchmark`까지 하나의 파이프라인으로 연결하면 이 공고의 인프라·트레이닝 두 영역을 동시에 증명할 수 있습니다. Babechat이 실제로 요구하는 vLLM/SGLang, alignment training, distributed training, quantization과 거의 그대로 대응합니다. ([LinkedIn][1])

**종합적으로는 기술 적합도 9.5/10, 현재 지원 현실성은 약 7/10 정도**로 봅니다. 경력 조건만 아니었다면 지금까지 나온 국내 공고 중에서도 상당히 우선순위가 높은 편입니다. 특히 **“LLM 모델 엔지니어 → ML Infra/LLM Systems 엔지니어” 사이를 모두 경험하고 싶은 경우**에는 업무 내용 자체가 매우 좋은 포지션입니다.

[1]: https://kr.linkedin.com/jobs/view/llm-ai-engineer-at-babechat-ai-4448968060 "Babechat.ai LLM/AI Engineer(경기도, 대한민국) 채용중 | LinkedIn"
[2]: https://babechat.career.greetinghr.com/ko/o/186616?utm_source=chatgpt.com "LLM/AI 엔지니어 (LLM/AI Engineer) 채용"
[3]: https://babechat.career.greetinghr.com/ko/apply?utm_source=chatgpt.com "주식회사 베이비챗 채용"
