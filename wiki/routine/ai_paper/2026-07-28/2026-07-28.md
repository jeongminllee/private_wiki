# AI 엔지니어 데일리 브리핑

**2026년 7월 28일 화요일**

## 오늘의 핵심 흐름

오늘은 **초대형 오픈 웨이트 모델의 공개**, **vLLM의 KV 캐시 계층화**, **장기 실행 에이전트의 컨텍스트 처리 최적화**가 하나의 흐름으로 이어집니다.

모델 성능만으로는 차별화하기 어려워지고 있습니다. 실제 경쟁력은 모델을 제한된 GPU에 어떻게 배치하는지, 반복되는 프롬프트와 컨텍스트를 얼마나 재사용하는지, 에이전트의 권한과 실행 경로를 어떻게 통제하는지에 달려 있습니다.

---

## 1. 필독 | Kimi K3: 2.8T 오픈 웨이트 모델이 공개됐다

### 무엇이 바뀌었나

Moonshot AI가 Kimi K3의 전체 모델 가중치와 배포 정보를 공개했습니다. Kimi K3는 총 2.8조 파라미터, 토큰당 1,040억 활성 파라미터를 사용하는 MoE 모델입니다. 전체 896개 전문가 중 토큰마다 16개를 선택하며, 최대 컨텍스트 길이는 1,048,576토큰입니다.

가중치는 MXFP4, 활성값은 MXFP8을 사용하는 양자화 인지 학습 구조이며, 텍스트·이미지·비디오를 처리하는 네이티브 멀티모달 모델로 소개됐습니다. ([Hugging Face][1])

### 왜 중요한가

Kimi K3는 단순히 “큰 오픈 모델”이 아닙니다. 초대형 모델에서도 다음 조합이 현실적인 설계 방향이 되고 있음을 보여줍니다.

* 극단적으로 높은 MoE 희소성
* 학습 단계부터 적용한 FP4 계열 양자화
* 100만 토큰 컨텍스트
* 장시간 코딩·도구 실행을 전제로 한 에이전트 학습
* 어텐션 비용을 줄이는 KDA와 Gated MLA 혼합

즉, 모델 접근성의 장벽은 낮아졌지만 **서빙 인프라 장벽은 오히려 높아졌습니다.**

### 5×B200 환경에서의 현실성

이전에 이야기한 5×B200 환경을 기준으로 보면, B200 한 장의 HBM은 180GB이므로 전체 GPU 메모리는 약 900GB입니다. Kimi K3의 2.8T 파라미터를 순수 4비트로만 계산해도 약 1.4TB가 필요합니다.

따라서 5×B200에는 가중치만으로도 들어가지 않습니다. 8×B200은 총 1.44TB이지만, 양자화 메타데이터·런타임 버퍼·KV 캐시·CUDA Graph 공간까지 고려하면 단일 8-GPU 노드 역시 여유가 거의 없습니다. 이는 공식 모델 사양과 NVIDIA의 B200 메모리 사양을 이용한 단순 용량 계산입니다. ([Hugging Face][1])

현실적인 선택지는 다음과 같습니다.

1. API로 모델 성능과 에이전트 특성을 먼저 평가
2. 여러 노드에서 Expert Parallel과 Tensor Parallel을 결합
3. CPU·NVMe expert offloading을 사용하되 지연시간 손실 측정
4. K3 자체보다 동일 아키텍처 계열의 더 작은 모델로 실험
5. 모델 다운로드 전에 체크포인트 크기와 로컬 스토리지 대역폭 점검

### 실무상 주의할 점

Kimi K3는 항상 thinking이 활성화되며, 멀티턴 도구 호출에서는 이전 응답의 `reasoning_content`와 `tool_calls`를 포함한 전체 assistant 메시지를 다시 전달해야 합니다. 일반적인 OpenAI 호환 클라이언트가 `content`만 보존하면 멀티턴 성능이 깨질 수 있습니다. ([Hugging Face][1])

공개 벤치마크도 그대로 모델 간 절대 비교에 사용하면 곤란합니다. Kimi K3는 Kimi Code, Claude 계열은 Claude Code, GPT 계열은 Codex 등 서로 다른 에이전트 harness로 평가된 항목이 있으며, 일부 모델에는 거절 또는 fallback이 발생했습니다. ([Hugging Face][1])

**후속 관찰 포인트:** vLLM·SGLang의 실제 다중 노드 배포 레시피, Expert Parallel 통신량, 독립 벤치마크, Kimi K3 License의 상업적 제약.

**원문**

* **[모델 카드]** Kimi K3 아키텍처·벤치마크·배포 정보. ([Hugging Face][1])
* **[GitHub]** Moonshot AI 공식 저장소 및 모델 프로젝트. ([GitHub][2])
* **[하드웨어 사양]** NVIDIA B200 GPU당 180GB HBM 사양. ([NVIDIA Docs][3])

---

## 2. 필독 | vLLM 0.26.0: KV 캐시가 GPU 내부 기능에서 스토리지 계층으로 확장된다

### 무엇이 바뀌었나

vLLM 0.26.0이 7월 27일 공개됐습니다. 212명의 기여자가 참여한 411개 커밋 규모의 릴리스입니다.

핵심 변화는 다음 세 가지입니다.

* KV 캐시 그룹별로 서로 다른 attention backend 선택
* CPU·객체 스토리지를 포함한 KV offloading과 secondary tier 강화
* DeepSeek-V4, speculative decoding, MoE routing 최적화

KV offloading에는 CPU 캐시 read/write 지표, 동기·비동기 tier lookup 지연시간, object-store 보조 계층, 데이터 병렬 replica를 인식하는 캐시 배치 기능 등이 추가됐습니다. ([GitHub][4])

### 왜 중요한가

KV 캐시는 더 이상 “GPU 메모리에 남아 있으면 재사용하는 부가 기능”이 아닙니다.

장문 RAG와 에이전트 트래픽에서는 KV 캐시가 다음과 같은 계층형 데이터가 됩니다.

```text
GPU HBM
  ↓
CPU DRAM
  ↓
로컬 NVMe
  ↓
원격 Object Storage
```

이 구조가 제대로 동작하려면 캐시 적중률뿐 아니라 캐시를 가져오는 시간이 재계산보다 짧은지를 판단해야 합니다. 적중률이 높더라도 object storage에서 KV 블록을 불러오는 동안 GPU가 멈춘다면 성능은 오히려 악화될 수 있습니다.

### 실무 적용

0.26.0을 평가할 때는 다음 지표를 분리해야 합니다.

* GPU·CPU·원격 계층별 cache hit rate
* 동기 및 비동기 tier lookup latency
* KV read/write bandwidth
* cache promotion·demotion 횟수
* TTFT P50·P95·P99
* preemption과 recomputation 비율
* prefix별 재사용 토큰 수
* 캐시를 가져온 시간과 재계산 예상 시간의 차이

특히 서로 다른 attention 구조를 섞는 hybrid model에서는 KV 그룹별 backend 선택이 유용할 수 있습니다. Sliding Window Attention과 full attention을 동일한 backend에 억지로 맞추는 비효율을 줄일 수 있기 때문입니다.

### 주의할 점

이번 릴리스는 변경 범위가 매우 넓습니다. 모델 지원, Transformers 5.13, FlashInfer, NIXL, 양자화, Rust frontend가 동시에 바뀌었습니다. 운영 환경에서는 바로 교체하기보다 고정 트래픽을 재생하는 canary 환경에서 검증하는 편이 안전합니다.

보안 측면에서는 pickle 역직렬화 경로 제거, 경로 정보 노출 방지, 요청당 prompt 목록 제한, 정규식 컴파일 시간 제한 등이 포함됐습니다. 외부 사용자에게 OpenAI 호환 endpoint를 공개하는 서비스라면 성능 변화뿐 아니라 이 보안 수정도 업그레이드 사유가 됩니다. ([GitHub][4])

**후속 관찰 포인트:** Kimi K3와 DeepSeek-V4의 안정성, object-store tier의 실제 손익분기점, hybrid attention 모델의 backend 조합, 0.26.x 패치 릴리스.

**원문**

* **[GitHub 릴리스]** vLLM v0.26.0 전체 릴리스 노트. ([GitHub][4])

---

## 3. 중요 | Claude Opus 5: 토큰 가격보다 ‘성공한 작업당 비용’으로 평가해야 한다

### 무엇이 바뀌었나

Anthropic은 7월 24일 Claude Opus 5를 출시했습니다. API 가격은 입력 100만 토큰당 5달러, 출력 100만 토큰당 25달러입니다. 기본 모드보다 약 2.5배 빠른 Fast mode도 제공되지만 가격은 두 배입니다. ([Anthropic][5])

Opus 5는 effort 설정으로 추론량을 조절할 수 있으며, Anthropic은 Frontier-Bench와 CursorBench에서 이전 Opus 4.8보다 작업당 비용 대비 성능이 크게 개선됐다고 주장합니다. 해당 결과는 Anthropic 내부 실행과 특정 agent harness를 기반으로 합니다. ([Anthropic][5])

### 엔지니어 관점

에이전트 모델의 경제성을 비교할 때는 다음 계산이 더 적합합니다.

```text
성공한 작업당 비용
=
총 API 비용
+ 재시도 비용
+ 도구 실행 비용
+ 인간 검토 비용
──────────────────
실제로 완료된 작업 수
```

출력 토큰 가격이 싼 모델도 잘못된 방향으로 20회 도구를 호출하거나 세 번 재시도하면 더 비쌀 수 있습니다. 반대로 고가 모델이 한 번에 작업을 완료하고 검증 코드까지 작성한다면 전체 비용은 낮아질 수 있습니다.

### 실무 적용

동일한 코드 작업을 다음 조건으로 반복 평가하는 것이 좋습니다.

* low·high·max effort
* 기본 모드와 Fast mode
* 모델별 최소 5회 반복
* 성공률과 부분 성공률
* wall-clock time
* tool-call 횟수
* 입력·출력·캐시 토큰
* 테스트 통과 여부
* 인간 수정 시간

Opus 5는 대화 도중 사용 가능한 도구를 변경해도 prompt cache가 무효화되지 않는 기능도 베타로 제공합니다. 권한 상승이 필요한 에이전트에서 처음에는 읽기 도구만 제공하고, 승인 이후 쓰기 도구를 추가하는 패턴에 활용할 수 있습니다. ([Anthropic][5])

### 주의할 점

안전 분류기에 걸린 요청은 Opus 4.8로 자동 fallback될 수 있습니다. 따라서 로그에 요청 모델뿐 아니라 **실제로 실행된 모델과 fallback 여부**를 남겨야 합니다. 그렇지 않으면 같은 모델을 평가했다고 생각하면서 서로 다른 모델의 결과를 섞게 됩니다. ([Anthropic][5])

**후속 관찰 포인트:** effort별 비용 곡선, 반복 실행 분산, 자동 fallback 관측 기능, 실제 코드베이스에서의 장기 작업 성공률.

**원문**

* **[공식 발표]** Claude Opus 5 기능·가격·평가 결과. ([Anthropic][5])
* **[시스템 카드 안내]** Opus 5 안전성 및 사이버 평가 설명. ([Anthropic][5])

---

## 4. 중요 | Skill Self-Play: 에이전트의 ‘스킬’을 학습 데이터 생성기로 사용한다

### 무엇이 바뀌었나

7월 24일 공개된 Skill Self-Play는 proposer, solver, skill controller를 함께 진화시키는 강화학습 프레임워크입니다.

각 스킬은 특정 유형의 문제를 생성하고 검증하는 계약 역할을 합니다. Proposer가 스킬에 맞는 과제를 만들고, 검증기를 통과한 과제 중 현재 solver가 풀기에는 적당히 어려운 문제를 학습 데이터로 선택합니다. 학습이 끝난 solver는 추론 시 스킬 라이브러리를 사용하지 않고 일반 프롬프트만으로 실행됩니다. ([arXiv][6])

### 왜 중요한가

기존 self-play에는 두 가지 문제가 있습니다.

* 고정된 환경에서는 검증은 쉽지만 문제 다양성이 부족함
* 자유롭게 문제를 생성하면 다양성은 높지만 정답 검증이 어려움

Skill Self-Play는 스킬별로 문제 생성 범위와 검증 규칙을 제한해 이 둘 사이의 중간 지점을 만듭니다.

이때 스킬은 런타임 플러그인이나 에이전트 메모리가 아니라 **검증 가능한 학습 커리큘럼 생성기**에 가깝습니다.

### 실무 적용

사내 도구 호출 모델을 학습한다면 스킬을 다음 형태로 정의할 수 있습니다.

```text
skill_name: github_issue_triage

input_contract:
  - issue body
  - repository metadata

allowed_tools:
  - search_code
  - read_issue
  - list_commits

task_generator:
  - 재현 가능한 버그 분류 문제 생성
  - 관련 파일 탐색 문제 생성

verifier:
  - 허용되지 않은 도구 호출 여부
  - 근거 파일 존재 여부
  - 정답 label과 일치 여부

difficulty_signal:
  - 현재 solver 성공률
  - tool-call 수
  - 불필요한 탐색 횟수
```

모델이 이미 잘 푸는 문제는 줄이고, 실패율이 지나치게 높은 문제도 제외해 학습 경계 부근의 샘플을 유지하는 것이 핵심입니다.

### 주의할 점

공개 결과는 API-Bank, BFCL, ZebraLogic을 중심으로 측정됐습니다. 일부 모델에서는 큰 향상이 보고됐지만, 이는 초기 tool-use 정렬 상태가 낮았던 모델에서 나타난 결과일 가능성도 있습니다.

공개 학습 스크립트는 8개 GPU를 가정하며, proposer가 GPU 0~3, solver가 전체 8개를 사용합니다. 사용자의 5-GPU 환경에서는 프로세스 배치와 rollout 동시성을 수정해야 합니다. ([GitHub][7])

**후속 관찰 포인트:** 더 복잡한 실제 도구 환경에서의 일반화, verifier gaming, 스킬 라이브러리의 중복과 품질 저하, 4~5 GPU 축소 재현성.

**원문**

* **[논문]** Skill Self-Play: Pushing the Frontier of LLM Capability with Co-Evolving Skills. ([arXiv][6])
* **[GitHub]** 공식 학습 코드와 실행 스크립트. ([GitHub][7])

---

## 5. 연구 | 장기 실행 에이전트의 병목은 컨텍스트 길이보다 ‘컨텍스트 재작성’이다

두 시스템 논문이 서로 다른 방향에서 같은 문제를 다룹니다.

### SmoothAgent: 컨텍스트 변환을 미리 실행

장기 실행 에이전트는 대화가 길어지면 요약, 메시지 격리, 외부 저장소 offloading 같은 context engineering을 수행합니다. 문제는 컨텍스트가 수정될 때 기존 KV 캐시가 무효화되고 전체 prefix를 다시 prefill해야 한다는 점입니다.

SmoothAgent는 요약이나 offloading처럼 미래 토큰과 독립적으로 처리할 수 있는 작업을 미리 비동기로 실행하고, 변환된 컨텍스트의 KV 캐시까지 준비해둡니다. 논문은 특정 조건에서 TTFT를 최대 11.9배 줄였다고 보고합니다. ([arXiv][8])

실무에서는 요약 작업을 agent loop 내부의 동기 함수로 실행하기보다 별도의 낮은 우선순위 serving request로 취급할 수 있습니다.

```text
현재 작업 요청 ────────────────▶ 응답 생성
       │
       └─▶ 다음 요약 후보 생성
              └─▶ 요약 컨텍스트 prefill
                     └─▶ 필요 시 즉시 교체
```

### PEEK: 대기열 자체를 KV 캐시 예측 신호로 사용

PEEK는 현재 캐시에 무엇이 들어 있는지만 보는 대신, 대기 중인 요청들이 어떤 prefix를 공유하는지 radix tree로 관리합니다.

같은 시스템 프롬프트나 문서를 공유하는 요청을 가까이 배치하고, 곧 대규모 요청 집단이 사용할 KV 블록은 eviction에서 보호합니다. SGLang과 vLLM에서 prefix 구조가 강한 워크로드를 대상으로 캐시 적중률, TTFT, 처리량이 크게 개선됐다고 보고했습니다. 반대로 공유 prefix가 없는 워크로드에서는 기존 방식과 비슷한 수준이었습니다. ([arXiv][9])

### 엔지니어 관점

두 논문을 합치면 에이전트 서빙에서 다음 원칙을 얻을 수 있습니다.

1. 미래에 사용할 컨텍스트 변환을 미리 계산한다.
2. 대기열을 단순 FIFO가 아니라 prefix 구조로 본다.
3. 현재 cache hit보다 미래의 cache reuse를 예측한다.
4. 재사용 최적화가 singleton 요청을 굶기지 않도록 fairness lane을 둔다.
5. 평균 TTFT뿐 아니라 가장 오래 기다린 요청의 지연시간을 측정한다.

### 주의할 점

두 결과 모두 “최대” 개선치이며, 공유 prefix가 많고 KV 캐시 압력이 높은 조건에서 이점이 커집니다. 사용자 프롬프트가 거의 겹치지 않거나 요청량이 낮다면 scheduler 복잡성만 증가할 수 있습니다.

또한 미리 요약하거나 KV를 생성하면 사용되지 않는 speculative work가 발생합니다. 절약한 prefill 시간과 낭비한 선행 연산량을 함께 측정해야 합니다.

**후속 관찰 포인트:** vLLM·SGLang 메인라인 통합 여부, prefix-rich 워크로드 탐지, 비동기 작업의 GPU 간섭, tenant 간 캐시 격리와 timing side channel.

**원문**

* **[논문]** SmoothAgent: Lookahead Context Engineering. ([arXiv][10])
* **[논문]** PEEK: Predictive Queue-Informed KV Cache Management. ([arXiv][11])
* **[GitHub]** PEEK 공식 구현 링크가 포함된 논문 페이지. ([arXiv][9])

---

## 6. 실무 | GitHub Copilot 앱과 CLI의 권한 정책이 분리됐다

### 무엇이 바뀌었나

GitHub은 7월 27일부터 Copilot 앱 접근 정책을 Copilot CLI 정책과 분리했습니다. 기업과 조직은 앱과 CLI를 각각 독립적으로 활성화할 수 있습니다.

Copilot 앱과 cloud agent는 기존 `managed-settings.json` 정책도 적용받습니다. 관리자는 사용할 수 있는 플러그인과 marketplace, 명령 실행·파일 접근·URL 요청 전 승인 우회 가능 여부 등을 중앙에서 지정할 수 있습니다. ([The GitHub Blog][12])

### 왜 중요한가

코딩 에이전트의 권한 경계는 모델 단위가 아니라 **실행 클라이언트 단위**로 관리해야 합니다.

같은 Copilot 모델을 사용해도 다음 환경의 위험 수준은 다릅니다.

* IDE 안에서 코드만 제안
* 로컬 CLI에서 셸 명령 실행
* 격리된 cloud workspace에서 작업
* GitHub 앱이 이슈를 받아 PR 생성
* 플러그인이나 MCP 서버를 통한 외부 시스템 접근

따라서 “Copilot 사용 허용”이라는 단일 정책은 충분하지 않습니다.

### 실무 적용

조직에서 코딩 에이전트를 운영한다면 다음 정책을 분리하는 편이 좋습니다.

* 클라이언트별 접근 허용
* plugin·MCP allowlist
* 셸 명령 승인 정책
* 네트워크 및 URL 접근
* secret 접근 차단
* 격리 workspace 사용
* 직접 push 금지와 PR-only 반영
* 리뷰·테스트·감사 로그 보존

특히 새 Copilot 앱 정책은 기본값이 **Enabled everywhere**입니다. 기존에 CLI만 통제하고 있던 조직은 의도하지 않게 새로운 클라이언트가 활성화됐는지 확인해야 합니다. ([The GitHub Blog][12])

**후속 관찰 포인트:** cloud agent의 승인 우회 정책 범위, MCP 정책 통합, repository별 세분화, agent-generated PR의 감사 데이터.

**원문**

* **[릴리스 노트]** Copilot 앱 전용 접근 정책. ([The GitHub Blog][12])
* **[릴리스 노트]** Copilot 앱·cloud agent의 enterprise managed settings. ([The GitHub Blog][13])

---

# 오늘 꼭 볼 것 3개

### 1. Kimi K3 모델 카드

성능 순위보다 **2.8T 모델을 MXFP4·MoE·1M context로 어떻게 구성했는지**를 보는 것이 중요합니다. 특히 16-of-896 expert routing과 배포 요구사항을 확인할 가치가 큽니다. ([Hugging Face][1])

### 2. vLLM 0.26.0 릴리스 노트

KV offloading, object-store tier, hybrid attention backend, 보안 수정이 한 번에 들어간 대규모 릴리스입니다. vLLM을 사용 중이라면 업그레이드 전후 벤치마크 항목을 설계할 근거가 됩니다. ([GitHub][4])

### 3. Skill Self-Play 코드

스킬을 에이전트 런타임 기능이 아니라 **자동 학습 데이터 생성·검증 계약**으로 바라보는 설계가 실용적입니다. 향후 도구 호출 파인튜닝 프로젝트에 적용하기 좋습니다. ([GitHub][7])

---

# 오늘의 실습: Prefix 구조에 따른 vLLM 성능 차이 측정

**목표:** 단순 QPS가 아니라 요청 대기열의 prefix 구조가 TTFT와 KV 캐시 재사용에 미치는 영향을 확인합니다.

현재 실행 가능한 모델 하나를 vLLM으로 올리고 세 종류의 트래픽을 만듭니다.

### 트래픽 A: 공유 prefix 없음

각 요청이 서로 다른 시스템 프롬프트를 사용합니다.

### 트래픽 B: 8K 공유 prefix

모든 요청이 같은 시스템 프롬프트와 문서를 공유하고 마지막 질문만 변경합니다.

### 트래픽 C: 4개 prefix cluster

요청을 네 그룹으로 나누고, 그룹별로 서로 다른 8K prefix를 공유합니다. 요청 순서는 그룹이 교차하도록 섞습니다.

측정 항목은 다음과 같습니다.

```text
request_count
input_tokens
output_tokens
TTFT_p50
TTFT_p95
TTFT_p99
TPOT_p50
prefix_cache_hit_rate
reused_tokens
preemption_count
GPU_KV_cache_usage
requests_per_second
```

동시 요청 수는 `1 → 8 → 32 → 64`로 증가시킵니다. 각 실험은 동일한 요청과 순서로 최소 세 번 반복합니다.

가장 중요한 비교는 B와 C입니다. 전체 공유 토큰량이 비슷해도 C의 요청을 무작위로 교차시키면 캐시가 반복해서 밀려날 수 있습니다. 그다음 C의 요청을 prefix cluster별로 묶어 실행해 TTFT와 cache hit rate가 얼마나 달라지는지 확인합니다.

이 실험을 해두면 이후 PEEK 같은 queue-aware scheduler, vLLM 0.26.0의 KV tiering, 에이전트별 공통 system prompt 설계를 평가할 기준 데이터를 얻을 수 있습니다.

* [Financial Times](https://www.ft.com/content/aa856a9d-b57c-4adf-b5aa-d1b22f9be3b2?utm_source=chatgpt.com)
* [businessinsider.com](https://www.businessinsider.com/white-house-kimi-k3-moonshot-ai-distillation-2026-7?utm_source=chatgpt.com)

[1]: https://huggingface.co/moonshotai/Kimi-K3 "moonshotai/Kimi-K3 · Hugging Face"
[2]: https://github.com/moonshotai?utm_source=chatgpt.com "Moonshot AI"
[3]: https://docs.nvidia.com/enterprise-reference-architectures/hgx-ai-factory/latest/components.html?utm_source=chatgpt.com "Components — NVIDIA HGX AI Factory"
[4]: https://github.com/vllm-project/vllm/releases "Releases · vllm-project/vllm · GitHub"
[5]: https://www.anthropic.com/news/claude-opus-5 "Introducing Claude Opus 5 \ Anthropic"
[6]: https://arxiv.org/abs/2607.22529?utm_source=chatgpt.com "Skill Self-Play: Pushing the Frontier of LLM Capability with Co-Evolving Skills"
[7]: https://github.com/Qwen-Applications/skill-self-play "GitHub - Qwen-Applications/skill-self-play: Skill Self-Play: Pushing the Frontier of LLM Capability with Co-Evolving Skills · GitHub"
[8]: https://arxiv.org/abs/2607.00151?utm_source=chatgpt.com "SmoothAgent: Efficient Long-Horizon LLM-Based Agent Serving with Lookahead Context Engineering"
[9]: https://arxiv.org/html/2607.02525v1 "PEEK: Predictive Queue-Informed KV Cache Management for LLM ServingCode: https://github.com/xiexbing/peek"
[10]: https://arxiv.org/html/2607.00151v1 "SmoothAgent: Efficient Long-Horizon LLM-Based Agent Serving with Lookahead Context Engineering"
[11]: https://arxiv.org/abs/2607.02525?utm_source=chatgpt.com "[2607.02525] PEEK: Predictive Queue-Informed KV Cache ..."
[12]: https://github.blog/changelog/2026-07-27-manage-github-copilot-app-access-with-a-dedicated-policy/ "Manage GitHub Copilot app access with a dedicated policy - GitHub Changelog"
[13]: https://github.blog/changelog/2026-07-27-enterprise-managed-settings-now-apply-to-the-github-copilot-app/ "Enterprise managed settings in the GitHub Copilot app and Copilot cloud agent - GitHub Changelog"
